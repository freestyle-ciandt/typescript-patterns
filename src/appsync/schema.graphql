type Query {
  getVehicle(id: String!): Vehicle
}

type Vehicle {
  id: String!
  category: String!
  seats: Int!
  publishedAt: Int!
  available: Boolean!
}

type Mutation {
  desingPattern(route: String!, message: String!): MessageData
}

type MessageData{
  route: String!
  message: String!
}